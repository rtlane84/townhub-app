# TownHub release-critical audit — 2026-07-16

This is the current evidence ledger for the release audit. Documentation claims are not treated as live verification. No provider or production configuration was changed during this audit.

| Gate | Repository evidence | Live-dashboard evidence | Test result | Status | Exact remaining action |
|---|---|---|---|---|---|
| Build/type safety | `@workspace/api-client-react` now declares its Node test types and its TypeScript config opts into them. `pnpm run audit:health` passes. | N/A | `pnpm run typecheck` passes; `git diff --check` passes. | Complete | Keep the package-local Node test type declaration when updating generated client tooling. |
| Auth/tenancy | API routes use `requireAuth`, `requireAdmin`, and `authorizeBusinessOwnerOrAdmin`; negative security regression tests exist. Account deletion route and contract tests exist. | Not independently inspected in this pass. | Static review only. | Needs live verification | Verify Clerk staging/production instances, native Apple/Google return paths, and run unauthenticated, wrong-owner, wrong-role, and disabled-user tests. |
| Payments | Connect routes and Billing routes are separate; raw webhook handling and idempotency code are present; Stripe E2E is explicitly gated by `E2E_STRIPE_CHECKOUT`. | Staging Sandbox Connect completed hosted Checkout, webhook materialization, and refund reconciliation. Stripe dashboard destinations and live-mode alerting were not independently inspected in this pass. | July 16 hosted Checkout **1/1** and full-refund workflow **1/1** passed against staging test mode. | Needs live verification | Verify Connect/Billing webhook destinations and alerting in Stripe test and live modes. |
| Database/recovery | Drizzle schema, RLS lockdown, R2 backup, and ephemeral restore-drill workflows are checked in and documented. | Backup/restore run IDs and Supabase projects require dashboard confirmation. | No database push or production mutation performed. | Needs live verification | Confirm the recorded GitHub Actions runs, archive contents, retention, and staging/production project separation; verify media-storage coverage. |
| Deployments | `wrangler.toml` defines staging/production environments; docs record Railway `develop`/`main` and Cloudflare build settings. | Cloudflare account confirms separate `townhub-app` and `townhub-production` Workers and `townhub-production-backups` R2 bucket. Detailed build configuration remains unverified because the dashboard is covered by a cookie-preference overlay. | Static configuration review only. | Needs live verification | Inspect both dashboards and record repository, branch, build command, domains, and non-secret variable names without copying values. |
| Monitoring | Health checks, Sentry instrumentation, backup/restore workflows, uptime workflow, and log-drain documentation exist. | Sentry, Better Stack, log drain, alert destinations, and test alerts were not independently inspected in this pass. | No alert test run. | Needs live verification | Verify environment tags, monitors, drains, recipients, and test-alert receipts; update runbooks with timestamps. |
| Automated unit/API tests | Frontend and API suites cover account deletion, authorization, order/payment gates, subscriptions, webhook configuration, and rate limits. | N/A | Frontend suite passes; API suite passes **453/453** (the rate-limit listener test was re-run outside the sandbox). | Complete | Run staging E2E separately; unit/API success does not replace provider verification. |
| Web E2E | Playwright suites cover public, customer, owner, admin, checkout, refund, and workflow paths. Auth fixtures are intentionally preserved and uncommitted. | `https://staging.townhub.io` passed public, owner, admin, hosted Stripe Checkout, and refund checks. | July 16 public smoke **3/3**, owner **4/4**, admin **2/2**, Stripe checkout **1/1**, and refund **1/1** passed; previously recorded broader public/guest/owner/admin run was 11/11. | Complete for current web release gate | Keep the fixtures fresh and rerun before promotion; signed native/device verification remains separate. |
| iOS/TestFlight | Capacitor config and release scripts exist; privacy manifest and App Store metadata docs exist. | Apple signing, App Store Connect record, certificates, provisioning, and device sessions were not independently inspected in this pass. | No signed archive or physical-iPhone matrix run in this pass. | Open | Produce a signed archive, install on physical iPhones, run Apple/Google/push/checkout/owner/admin matrix, then submit TestFlight. |

## Audit rules

- Do not mark a gate complete from a documentation claim alone.
- Do not expose secrets or copy provider secret values into this ledger.
- Keep `tests/e2e/fixtures/.auth/admin.json` and `owner.json` untouched unless fixtures are intentionally regenerated.
- Production changes require explicit confirmation; staging and read-only verification are the default.
