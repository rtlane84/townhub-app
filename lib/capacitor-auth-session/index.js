/**
 * Cap sync entry for the native AuthSession plugin.
 * JS consumers should import AuthSession from the townhub app
 * (`@/lib/native-auth-session`) so Vite can resolve `@capacitor/core`.
 */
export {};
