{
  "name": "workspace",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.11.1",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "audit:health": "pnpm --filter @workspace/scripts run health",
    "audit:health:bundle": "pnpm --filter @workspace/townhub run build && pnpm --filter @workspace/scripts run health -- --bundle",
    "release:check-env": "node scripts/validate-release-env.mjs",
    "release:ios:staging": "node scripts/ios-prepare.mjs staging",
    "release:ios:production": "node scripts/ios-prepare.mjs production",
    "release:ios:bump-build": "node scripts/bump-ios-version.mjs --build",
    "release:ios:set-version": "node scripts/bump-ios-version.mjs --marketing",
    "release:ios:open": "pnpm --filter @workspace/townhub run ios:open",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug"
  },
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
