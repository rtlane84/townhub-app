Update LocalOrderHub with the following improvements. Do not rebuild the app from scratch. Extend the existing structure and preserve existing routes, data, auth, dashboards, and styling unless changes are required.

Work in phases. After each phase, verify the app builds and routes still work before moving to the next phase. If a phase is too large, finish the highest-priority items first and clearly report what remains.

1. Mobile dashboard navigation
Add mobile-friendly navigation for business owner and admin dashboards.
- Dashboard sidebars should not disappear without a fallback.
- Add a mobile menu button/drawer or bottom nav for dashboard pages.
- Business owners must be able to access overview, orders, products, categories, and settings on phones.
- Admins must be able to access overview, businesses, orders, users, events, highlights, and settings on phones.

2. Pickup and delivery settings
Each business must be able to independently choose:
- pickup enabled
- delivery enabled
- both pickup and delivery
- neither, if the business only accepts quote/request/appointment inquiries

In Business Settings, add:
- pickupEnabled toggle
- deliveryEnabled toggle
- deliveryFee
- deliveryRadiusMiles optional
- deliveryNotes
- pickupInstructions
- deliveryInstructions
- minimumOrderForDelivery optional

On storefront cards and business pages, clearly show:
- Pickup Available
- Delivery Available
- Pickup & Delivery
- Request Only

At checkout, only show fulfillment options the business actually supports.

3. Business order notifications
Add notification settings and order notification behavior.
- Each business should have orderNotificationEmail.
- When an order is placed, notify the business owner by email if configured.
- If no email provider is configured, log the notification instead of failing.
- Send/log customer order confirmation.
- Send/log customer status update when order status changes.
- Add notification fields/settings to Business Settings.
- Keep this modular so a real provider like Resend can be added later.

4. Homepage events calendar
Add a homepage section called “Community Calendar” or “Upcoming Local Events.”

Platform admin can create/edit/delete events.

Event fields:
- title
- date
- startTime optional
- endTime optional
- location
- description
- imageUrl optional
- relatedBusinessId optional
- eventType: COMMUNITY, FOOD_TRUCK, SEASONAL, SALE, HOLIDAY, MARKET, OTHER
- featured boolean

Show upcoming events on the homepage, sorted by soonest first.

Also create:
- /events page to list all upcoming events
- /dashboard/admin/events page where platform admin can manage events

5. Seasonal & Local Highlights section
Add a homepage section called “Seasonal & Local Highlights.”

This should show seasonal promotions, preorder windows, and local highlights such as:
- Mother’s Day flowers
- prom corsages
- fall mums
- Christmas wreaths
- graduation party catering
- holiday meal preorders
- greenhouse spring planting season
- hardware weekend sale

Platform admin should be able to manage these highlights from:
- /dashboard/admin/highlights

Highlight fields:
- title
- description
- imageUrl optional
- startDate
- endDate
- relatedBusinessId optional
- buttonText
- buttonUrl
- active boolean
- sortOrder

Show this section on the homepage below Browse by Category and above Featured Businesses or Today’s Specials.

6. Food truck location support
For businesses with businessType = FOOD_VENDOR and eventLocationEnabled = true, add food truck location support.

Business owner can manage:
- currentLocationName
- currentAddress
- latitude optional
- longitude optional
- locationDate
- startTime
- endTime
- locationNotes

Create a “Food Trucks Today” homepage section if any food truck locations are active today.

Food truck cards should show:
- truck name
- current location
- hours
- order button
- map button if address or coordinates exist

On the food truck storefront page, show:
- today’s location
- upcoming locations
- map link using address or coordinates
- pickup ordering tied to the selected location/time

Admin and business dashboard should allow managing food truck schedule entries.

7. Theme updates
Add platform-wide theme settings controlled by admin:
- primaryColor
- accentColor
- backgroundColor
- buttonColor

Default theme should use Clay-inspired blue and gold:
- primary blue: #1E3A8A
- gold accent: #F59E0B
- dark navy: #0F172A
- light background: #F8FAFC

Also allow each business to optionally set storefront:
- accentColor
- buttonColor
- bannerText

If business colors are blank, use platform defaults.


8. Keep customer order history as lower priority
Do not prioritize customer “My Orders” unless the above items are complete. Customers can rely on confirmation page and email/logged confirmation for MVP.

Additional addons: 
Add admin-manageable subscription and trial settings to LocalOrderHub. Do not rebuild the app. Extend the existing structure and use whatever Prisma schema/models best fit the current codebase.

Business requirement:
I need to control subscription pricing and trial settings from the admin dashboard rather than hardcoding them.

Admin must be able to:
- create and edit subscription plans
- set monthly subscription price
- set optional setup fee
- set optional transaction fee percentage
- set trial length in days
- mark plans active/inactive
- choose a default plan
- assign or override a plan when approving a business application

Business applications / onboarding:
- when a prospective business applies through “List Your Business”, they should be able to choose from active plans
- their selected plan should be stored with the application
- when I approve the application, the chosen plan should be attached to that business
- if the plan includes a trial, the business subscription should start in trialing status with the proper trial end date
- if no plan is selected, use the default active plan

Business owner billing page:
Add a billing page where the business owner can see:
- current plan
- monthly price
- setup fee if applicable
- trial status / trial end date
- subscription status

Admin UI:
Add an admin section or page for managing plans, pricing, trial length, and which plan is the default.

Implementation notes:
- use the current app’s architecture and choose the cleanest schema/models for plans and business subscriptions
- keep Stripe integration modular
- if Stripe is not configured, allow local/mock subscription records so the app still works in MVP mode
- do not break existing auth, business dashboards, or admin dashboards