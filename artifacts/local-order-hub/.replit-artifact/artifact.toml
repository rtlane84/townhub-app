kind = "web"
previewPath = "/"
title = "LocalOrderHub"
version = "1.0.0"
id = "artifacts/local-order-hub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23032

[services.development]
run = "pnpm --filter @workspace/local-order-hub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/local-order-hub", "run", "build" ]
publicDir = "artifacts/local-order-hub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23032"
BASE_PATH = "/"
