# Specific routes first — Netlify uses first match.
# Static OAuth bounce so Safari returns to the app without the SPA (avoids 404
# when React routes aren't deployed yet).
/native-sso-callback /native-sso-callback/index.html 200
/native-sso-callback/ /native-sso-callback/index.html 200
/* /index.html 200
